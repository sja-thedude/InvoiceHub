<% content_for :page_title, "Edit #{@invoice.invoice_number}" %>
<div class="space-y-5">
  <%= link_to @invoice, class: "text-sm text-slate-500 hover:text-slate-700" do %>← Back to <%= @invoice.invoice_number %><% end %>
  <h2 class="text-2xl font-bold text-slate-900">Edit <%= @invoice.invoice_number %></h2>
  <%= render "form", invoice: @invoice %>
</div>
