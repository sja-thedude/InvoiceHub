<% content_for :page_title, "Edit Project" %>
<div class="max-w-2xl mx-auto">
  <%= link_to @project, class: "text-sm text-slate-500 hover:text-slate-700" do %>← Back to <%= @project.name %><% end %>
  <h2 class="mt-2 text-2xl font-bold text-slate-900">Edit Project</h2>
  <div class="mt-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", project: @project %>
  </div>
</div>
