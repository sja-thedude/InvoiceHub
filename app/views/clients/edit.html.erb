<% content_for :page_title, "Edit Client" %>
<div class="max-w-2xl mx-auto">
  <%= link_to @client, class: "text-sm text-slate-500 hover:text-slate-700" do %>← Back to <%= @client.name %><% end %>
  <h2 class="mt-2 text-2xl font-bold text-slate-900">Edit Client</h2>
  <div class="mt-5 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", client: @client %>
  </div>
</div>
